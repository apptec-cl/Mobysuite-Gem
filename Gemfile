source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

group :development, :test do
  gem 'rspec'
  gem 'pry'
  gem 'dotenv'
end
# Specify your gem's dependencies in mobysuite.gemspec
gemspec